# Changelog

## Unreleased

- Add secure browser-confirmed `projects create` with automatic publishable-key delivery and readonly-token refresh.
- Add `analyticscli agent brief`, an evidence-first and machine-readable product context packet for coding agents.
- Add a stable MCP v1 stdio server with six bounded, read-only analytics tools.
- Report semantic activation events for project creation, CLI auth/onboarding, successful queries, and generated agent briefs.
- Expose `analyticscli --version`.
- Default acquisition analysis to event counts so privacy-safe aggregate pageviews are not presented as unique people.

All notable changes to `@analyticscli/cli` will be documented in this file.
