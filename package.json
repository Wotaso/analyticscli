{
  "name": "@prodinfos/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "prodinfos": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/index.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src --ext .ts",
    "test": "echo 'no tests for @prodinfos/cli'"
  },
  "dependencies": {
    "@prodinfos/config": "workspace:*",
    "commander": "^13.1.0"
  },
  "devDependencies": {
    "tsx": "^4.19.3"
  }
}
